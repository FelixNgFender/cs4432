// For the input data, assume the directory is named “Project1” in the same
// place from which the java program will run. Under this directory, there
// should be files F1, F2, … Note: TAs will not test for out-of-range record
// number or file number. Whatever is requested in the following commands is
// expected to be present under directory “Project1”.
